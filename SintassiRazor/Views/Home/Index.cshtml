@{
    ViewData["Title"] = "Home Page";

    string nome = "Yari";
    string cognome = "Narducci";

    int n = 10; 
    int somma= SommaNumeri(n);

    string frase = "nel mezzo del cammin di nostra vita";
    int conteggio = 0;

    foreach(char a in frase)
    {
        if (Char.ToLower(a)=='a')
        {
            conteggio++;
        }
    }

    var random = new Random();
    var numeri = new List<int>();
    for (int i = 0; i < 20; i++)
    {
        numeri.Add(random.Next(1, 91));
    }

    // Filtrare solo i numeri compresi tra 20 e 70

    var numeriFiltrati = numeri.Where(n => n >= 20 && n <= 70).ToList();

    DateTime data = DateTime.Now;

    string dataFormattata = data.ToString("yyyy-MM-dd HH:mm:ss");

    // Ottenere la data di oggi utilizzando la classe DateTime
    DateTime oggi = DateTime.Now;

    // Calcolare la data di 7 giorni fa
    DateTime data7GiorniFa = oggi.AddDays(-7);

    // Ottenere il nome del giorno della settimana di oggi
    string nomeGiornoSettimanaOggi = oggi.ToString("dddd");

    // Calcolare il giorno della settimana tra un anno nello stesso giorno e mese di oggi
    DateTime giornoSettimanaTraUnAnno = oggi.AddYears(1);
    string nomeGiornoSettimanaTraUnAnno = giornoSettimanaTraUnAnno.ToString("dddd");
}

@functions {
    public int SommaNumeri(int n)
    {
        int somma = 0;
        for (int i = 1; i <= n; i++)
        {
            somma += i;
        }
        return somma;
    }
    
  
}



<div>
    <h1 class="display-4 text-center">Razor </h1>
    <div>
        1. Dichiarare una variabile valorizzandola con il nostro nome.
        Dichiarare un'altra variabile valorizzandola con il nostro cognome.
        Stampare il nostro nome e cognome all'interno di un tag h2.
    </div>
    <h2>
        @nome @cognome
    </h2>
    <div>
        2. Creare una funzione che stampa la somma dei numeri che vanno da 1 a n (con n passato come parametro - n incluso)
        Stampare dentro uno span il risultato.
    </div>
    <span>La somma dei numeri da 1 a @n è: @somma</span>
    <div>
        3. Contare quante volte la lettera a è presente nalla frase "nel mezzo del cammin di nostra vita" e stampare questo valore in pagina
    </div>
    <div>
        Nella frase @frase sono contenuti @conteggio lettere a!!!
    </div>
    <div>
        4. Creare una List int di 20 numeri interi (generati random da 1 a 90, 1 e 90 inclusi) e mostrare in pagina solo i numeri compresi tra 20 e 70
    </div>
    <p>Numeri generati casualmente tra 1 e 90:</p>
    <ul>
        @foreach (var numero in numeri)
        {
            <li>@numero</li>
        }
    </ul>

    <p>Numeri compresi tra 20 e 70:</p>
    <ul>
        @foreach (var numero in numeriFiltrati)
        {
            <li>@numero</li>
        }
    </ul>
</div>

<div>
    5. Stampare in pagina la data e l'ora attuale (usando la classe DateTime) con il formato anno-mese-giorno ore:minuti:secondi
</div>
<p>
    La data di oggi è:<br />
    @dataFormattata
</p>

<div>
    6. Partendo dalla data di oggi (usando la classe DateTime) stampare in pagina<br />
    a. la data di 7 giorni fa<br />
    b. che giorno della settimana è oggi (il risultato dovrà essere il nome del giorno (Lunedì, Martedì, ...)<br />
    c. che giorno della settimana sarà nello stesso giorno e mese di oggi ma tra un anno (il risultato dovrà essere il nome del giorno (Lunedì, Martedì, ...)<br />
</div>
<p>Data di 7 giorni fa: @data7GiorniFa.ToString("dd/MM/yyyy")</p>
<p>Oggi è: @nomeGiornoSettimanaOggi</p>
<p>Il giorno della settimana tra un anno nello stesso giorno e mese di oggi sarà: @nomeGiornoSettimanaTraUnAnno</p>




